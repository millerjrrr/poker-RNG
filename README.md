## KYP RNG App

- Click the colored number to generate a number (0-99)
- Double-click the logo button to spawn a new window
- Close the window with the ✕ button or Esc

to run for testing use "python poker_rng_app.py"
to build use "py -3.11 -m PyInstaller --onefile --windowed --noconsole --name PokerRNG --icon icon.ico --add-data "logo.png;." poker_rng_app.py"
